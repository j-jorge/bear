subdirs(sdc)

configure_file(
  "${CMAKE_CURRENT_SOURCE_DIR}/desc2img.in"
  "${EXECUTABLE_OUTPUT_PATH}/desc2img"
  IMMEDIATE @ONLY)

install( FILES "${EXECUTABLE_OUTPUT_PATH}/desc2img"
  DESTINATION ${BEAR_FACTORY_INSTALL_EXECUTABLE_DIR}
  PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE
  GROUP_READ GROUP_EXECUTE
  WORLD_READ WORLD_EXECUTE )
